import { dark } from '@theme-ui/presets'

export default {
  ...dark,
  styles: {
    ...dark.styles,
  },
  buttons: {
    primary: {
      color: 'text',
      bg: 'primary',
    },
    secondary: {
      color: 'text',
      bg: 'secondary',
    },
  },
}
